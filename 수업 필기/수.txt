회원가입을 할때 가입 시간이 필요할 수 있다. 이떄 자주 사용하는게 now() , curdate() , curtime() 들을 많이 쓴다.
ifnull은 많이 씀. 나중에 집계할때

예시
select ifnull(null, 0);  만약에 어떤값이  null이면 null대신 0(기본값)을 사용하겠다.
ifnull(열값, 기본값)

if (조건, 참, 거짓) 
간단한 조건문을 함수로 만든것이다

더하기 기능은 숫자만 있다. 문자는 더하기 못함. 합치고 싶으면 다른함수 concat() 사용!

case에서 조건에 해당되지않으면 null값이 들어감.

한 컬럼에서 동등비교를 할때 case를 이용함
case는 order by 에도 사용가능
먼저 나오고싶은것부터 0, 1, ,2 ... 순서로 바꿔준다

case구는 select절에서 많이 사용함. 케이스마다 다른 출력값을 나오게 하기위해

count(*)은 전체 개수 세기
distinct는 보통 count에만 쓴다  - 중복을 제외해주므로 종류가 몇가진지 알기위해 사용.

커미션 없는 사람들 조회 순서
일단 커미션이 없는 사람들을 모두 select해준다
select *
from emp
where comm_pct is null
그런다음 select count(*) 와 같이 count를 추가해줌으로써 수를 세준다.

여러개 집계를 하면 하나의 결과만 나오므로 전체데이터와 함께 select할 수 없다.

순서 : select , from, where, group by  => 집계할떄만 사용.집계함수

~별 평균을 구할때 group by 를 사용함

그룹바이는 그룹으로 나눠서 집계하겠다

범주형 컬럼은 값을 나타내는것

순번은 select절에 있는 순서를 말한다.

format함수를 사용하면 값은 문자가 된다. 30000이 30,000이 되면 문자열이 된다. 그래서 정렬이 제대로 안될수도있다

순서:
select  (5)
from   (1)
where (2)
group by (3)  : 이 둘은 바껴도되는데 이렇게 쓰자
having    (4)   :이 둘은 바껴도되는데 이렇게 쓰자
order by (6)

having: 그룹을 나눴는데 내가 모든 그룹이 다 필요한게 아닐경우. having절을 이용해서 필요한 값만 가져온다

------------------------------
04_조인

나눠서 가지고있는 값을 같이 조회하기 위해서 조인이 필요하다
테이블을 나눈 이유 -> 데이터가 나뉘어 있으므로 
그러다보니 겹치는 정보가 없을까? 겹치는 정보로 조인시킴


erd : entitiy ~  데이터베이스 설계한것
테이블 이름 / pk컬럼 / 일반 컬럼

자식 컬럼에 있는 포린키는 부모 컬럼에 있는 프라이머리 키를 가지고있다.
자식이 부모키를 참조하지 않을수도 있다 -> 자식 컬럼은 널을 허용한다


테이블을 만들때 
on delete set null 설정은
참조하는 데이터 값이 사라지만 null로 값을 바꿔줘라
라는 뜻인데 이를 통해 부모 테이블에서 삭제할 수 있도록 해준다

set null은 포린키 컬럼이 넛널을 허용할때 사용가능하다

테이블 드롭도 불가능 => 참조되고있기때문에 삭제되지 못한다는 오류가 남

set foreign_key_checks는 마이 sql에서 주는 기능인데 0으로 설정하면 
포린키 상관없이 테이블 드롭이 된다

테이블이 나뉘어졌을때 문제는 