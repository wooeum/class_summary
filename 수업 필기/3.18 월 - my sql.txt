3.18 월 - my sql

관계형 데이터베이스 , NoSQL  => db를 어떻게 만들지에 대한 것

설치 과정
1.custom
2.mysql server 확장 3번하고나면 나오는 8.0.36 - x64 두가지 설치
데이터를 관리해주는 툴은 위에꺼 
밑에는 sql질문을 받아주는. 사용자를 위한 툴.

관계형데이터베이스 => 표형태로 저장하자
세개의 값이 하나의 관계를 가져서 하나의 데이터를 표현함.

데이터베이스에 데이터를 저장하고싶으면 표 구조부터 만들어야한다. 
표는 어떤 속성들로 구성할것인가

기본단위: 테이블(데이터 타입, 기본저장단위)

ex) 쇼핑몰 : 고객 데이터, 물건 데이터, 물건받아오는 회사 데이터, 주문내역 데이터... 등

독립된 데이터들을 entity라고한다
한 사람의 정보를 알고싶으면 행을 보면됨.
특정 속성을 보려면 열을 보면됨

CRUD(데이터넣고변경하고업데이트하고삭제한다)
create
read retreve
update
delete

crud를 해주는게 DBMS.
db는 하드디스크에 위치할것이다.
dbms는 db를 관리해주는 시스템
파일에 데이터를 저장해주고 가져오는 시스템이 dbms
명령을 내려주는 사람이 클라이언트고 명령내리는 언어가 SQL

클라이언트와 db서버는 네트워크로 연결되어있음.
이런 관계를 client-server관계 = cs구조

DQL - select 를 dql이라고도함. 질문하는것이기 때문에

권한은 table단위로 준다
모든 테이블에 대한 모든 권한

일반적으로 varchar를 사용함

문자열은 작은 따옴표로 감싼다

프라이머리키 = 중복된 값을 가질수없음
유니크 키 = 프라이머리랑 같지만 NULL값까지 가잘수있음. ex) 이메일을 반드시입력할 필요는 없지만 중복되면x
not null = null은 안됨
프라이머리키 = 유니크키 + not nulll

auto_increment는 mysql에 있는 기능이고 보통 primarykey와 같이 사용됨

권한은 루트에서 부여해줘야됨
