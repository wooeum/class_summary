도형은 보통 후처리 과정에서 사용함

blurring을 통해  noise를 사라지게 할 수 있다.
근데 실제 경계선만 따로 구분지을순 없다 => 경계선의 값도 같이 낮아지므로 전체적으로 흐려지는 현상이 생긴다

침식
모든 사물이 다 분리가 되는게 아니라. 배경은 0 , 물체는255처럼 섞인게 아니라 중간중간 섞여있을것이므로 경계를 얇게 만들고싶을때 사용.
어떻게 구현=> 값중에 255가 아닌 값이 있으면 pixcel을 0으로 바꿈

필터범위안에 0이 있으면 anchor점의 값은 0이된다.

wget  => web을 통해서 get요청을 받는다

try문말고 if문으로 써도됨. 
그리고 exception은 오류났다고 끝내는게 아니라 이어서 실행하려는게 목적임
