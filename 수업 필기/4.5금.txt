df.shape 으로 형태를 파악.
행은 값 하나하나, 열은 속성을 의미함

df.info() df자체에 대한 정보를 제공.

정렬후에 앞알파벳만 가져와서 정렬하는게 가능하다

정렬기준을 여러개로 주고싶을떄 리스트로 주면됨

sort는 query를 써주고 바로 뒤에 이어서 써서 사용하는데 값을 저장하고 싶은경우엔 변수에 저장시켜주기

기본은 0번방향 : 열방향
 axis = 1 => 1번방향 : 행방향

수치형만 가지고 계산 => numeric_only=True

agg에서는 보통 함수리스트를 넣어줘서 여러 함수를 같이 사용함

agg가 함수를 받았으니까 첫번째 파라미터(df['ari_time'])를 넘겨준다

그룹이 여러개면 불편하다 => 보기편하게 만들기위해서 나온게 pivot table
: 아까 보여주던걸 테이블형태로 보여주겠다.

--------------
04
