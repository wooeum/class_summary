<기본 절차>
1.연결 
connect함수 이용

4.연결 닫기
cursor, connection .close()
io에 대한 연결, 네트워크에 대한 연결을 닫는것

중간에 커밋하지 않고 모두 실행되면 커밋을 해주는건데
함수마다 cursor를 연결해야 되므로 함수에서 conn을 내가 하는게 아니라 conn을 받아서 해준다
그러고 커서만 끊고 돌아감. 커넥션은 살아있다

-------------------------

control + , :설정
control + ` :터미널 
control + shift + p (명령 팔레트)
	python select interpreter
	가상환경 선택

python -m venv env

여기엔 파이썬 기본 환경만 있음. 예전에 설치했던 주피터 랩같은 것은 없다.

오른쪽 상단 실행버튼 클릭 : py 파이썬 스크립트 실행

html : 웹

url 도메인
프로토콜 ://IP:port/파일경로
IP주소는 원래 숫자지만 어려워서 sports.news.naver과 같이 쉽게 이용함
주소를 몰라도 요청할 수 있게할수는 없을까? => 링크기능을 만듬

해외축구 -> 주소가 연결되어있다. 
해외축구를 클릭하면 알아서 해당 경로를 요청해줌
사용자입장에서는 다양한 주소를 몰라도 많은 서비스를 받을 수 있음.

링크기능 http
클릭하면 이동하고 주소 요청해주고 화면 만들어주는 애가 필요하다.

client.prg => web browser (ex) chrome, safari, ...
server.prg => web 서버 (ex) nginx, 아파치, IIS, ...

얘네들이 주고받을 텍스트를 만드는 문법을 정의한것이 HTML이다.

태그의 어트리뷰트.추가적인정보 <imp~> 같은것.
<a href="url"> 뉴스홈 </a> 처럼 정보를 알려주는것이 어트리뷰트

1. alt + 클릭 -> 커서 여러개 선택
2. 시프트 방향키로 여러단어 선택
3. ctrl + x -> 잘라내기
4. 시프트 + end -> 문장 맨뒤로 이동
5. ctrl + v -> 붙여넣기

