beuatifulsoup로 받아주고 파서를 lxml로 하면 태그로 찾아가는것이 가능하다.
json => 딕셔너리형태인데 문자열이다. 그러므로 데이터를 받아 딕셔너리로 변환해 사용하면 편리하다

params = { }  -> 요청파라미터로 딕셔너리 만들어서 제공해준것이다

selenium은 웹브라우저를 제어하는 애
크롬의 종류는 여러가지이므로 제어하는 방법이 다 다르다. 이때 driver필요!

element()는 하나만 찾는거라 보통 id찾을 때 사용.
elements()는 여러개

id, class
 
입력태그 - 요청파라미터를 사용자로부터 입력받을때 사용.
	  - 입력폼
	-name(태그속성) = value(사용자입력)

파싱 = html 번역하는 과정

sleep을 통해 기다리게되면 부족할수도 낭비될수도 있다. 그래서 셀리니움에서는 대기해주는 명령어가 있다.

거의 PRESENCE_OF ~ 를 쓴다. 내가 쓰려는 element가 올라올때까지 기다리는것.

초반만 나오고 더보기를 눌러야 다 볼수있는 곳에서의 글은 requests를 통해 가져올수 없다. 클릭을 하고 가져와야한다.


----------
일단 처음엔 pull.
내꺼에서 살짝 수정했으면 add까지만
다 끝나면 commit
