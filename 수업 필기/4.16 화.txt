데이터 전처리를 위해  opencv사용
전처리에선 분류하는게 제일 많다. 분류를 위한 특징들을 찾아봐야되는데
특징이 3개인것보단 1개인것이 편리하므로 grayscale을 사용한다.
처리하기 쉽게해주는 색을 표현하는 방식을 고른다.

일부만 처리하고 싶은 경우엔 같은 크기의 배열을 만들고 처리하고싶은 부분은 전부 255로 채우고 나머지는 0으로 채우고 mask에 이 행렬을 넣어준다


이진화는 그레이스케일로. 그리고 임계값을 줘서 두개의 값으로 바꿔줌
비슷한애들끼리 묶지만 극단적으로 단순화시킨것.

equalizeHist() 는 평활화해주는 함수
img_high에 img 평활화한것을 넣고 equ에서 imshow해주기

polylines에서 좌표들은 3차원 형태로 들어간다
pts는 ndarray로 좌표 묶어서 제공

후처리에서 많이 사용하는데. 우리가 찾은 것을 나타내기 위해 그려주기도함

직선은 anti aliasing안해도됨

이미지 resize는 그대로 유지한 상태에서 사이즈를 줄이는것. => 작게 만들고 그것을 원래 사이즈와 동일하게 키워서 보여줌
crop은 잘라내기. 
이미지는 nparray 이므로 slicing으로 crop할 수 있다.


<이동변환>
2*3이면 x,y에 하나는 뭐지 ==> 좌우로 이동하기위한 값. 1 고정.

이미지를 이동시키면 빈공간이 생기는데 이떄 생긴 공간을 0이나 다른 값으로 채워줘야한다

<전단 변환: 평행사변형 형태로 이동>

rotate는 자유도가 떨어짐 원하는 각도만큼 돌리지 못해서

딥러닝에서 resize는 거의 필수 => 이미지를 수집하고 사용하기위해 처리함수를 만들어준다. 이미지를 넣으면 분류를 해주거나 이미지를 찾아주는 등의 처리를 해주는데, 이미지를 받을때 동일한 크기의 이미지를 받아야한다. 

anchor점 떄문에 홀수개의 행렬을 사용함  => 한 점과의 좌표를 기준으로 계산을 하고 결과값을 준다. 이 점에 닻을 내린다는 의미로 anchor 점이라고 부른다.

