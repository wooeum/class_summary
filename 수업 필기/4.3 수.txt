가상환경 설치 python -m venv env
주피터랩 설치 pip install jupyterlab
판다스 라이브러리 설치 pip install pandas
최신버전 업그레이드 pip install pandas --upgrade
___________________________________________________________

파이썬에서 R을 대체하기 위해서 만들어진 패키지이다.
data가 들어있는 표 -> 데이터를 모아놓은 하나의 자료구조

기존의 리스트나 ~ 로는 만들려는 기능을 잘표현하기 힘들어서 만든 자료구조가 시리즈와 데이터프레임.
series는 리스트와 다름. 리스트+딕셔너리이다.

판다스는 내부적으로 numpy사용함. numpy가 제공하는 배열을 사용함 => np.array

시리즈는 행렬에서 한열이나 한 행을 의미함.
df는 행렬에서 모든 행 또는 열을 의미함

리스트만들듯이 시리즈를 만들어서 사용하는 경우는 많이 없고 열과 행을 조회해서 값을 다룰때 많이 사용된다.

시리즈에서는 index와 index이름이 모두 있다.인덱스 이름이 우리가 주는 키값.

시리즈 + 시리즈 => 같은 이름을 가진 원소들끼리 계산

마크다운 코드로 할때에는 ```   ``` 사용

RangeIndex(start=0, stop=7, step=1)  => range함수의 시리즈 인덱스 버전

series에서는 index name이 중복될 수 있다.

index 순번으로 조회하려면 => Series.iloc[순번]으로 조회하기

원본을 그대로 두고싶을때는 copy() 사용

보통 결측치는 있긴한데 값을 모르는 경우에 자주 사용된다.
없다의 경우도 있는데 직업을 조사하는데 직업이 없는 경우.

결측치는 float타입, object타입은 문자열타입이라고 생각하면 됨

arange는 range와 같은데 실수값도 가능.

----------------------------------------------
02
git status는 커밋까지 알려줌

df는 방향이 두개이므로 각 방향의 이름이 있다.

리브레 오피스

